class SizeEffect {
    constructor(start_scale = 1, end_scale = 1, shrink = false, pulse = false) {
        this.start_scale = start_scale
        this.end_scale = end_scale
        this.shrink = shrink
        this.pulse = pulse
    }
}